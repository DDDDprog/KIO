// KIO Fibonacci: Iterative Fibonacci Sequence
// Generates the first 10 terms.

let n = 10;
let a = 0;
let b = 1;

print "First " + n + " Fibonacci numbers:";

let i = 0;
while (i < n) {
    print a;
    let next = a + b;
    a = b;
    b = next;
    i = i + 1;
}
